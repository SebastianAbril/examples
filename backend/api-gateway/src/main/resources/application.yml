server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: catalog-service
          uri: ${CATALOG_SERVICE_URI}
          predicates:
            - Path=/catalog/**
          filters:
            - StripPrefix=1
        - id: consumerModule
          uri: http://localhost:8082/
          predicates:
            - Path=/consumer/**